# Machine-specific: the JDK Gradle itself runs on (Java 25, as required by build.gradle).
# Forward slashes work on Windows; point at the JDK home, not bin/java.exe.
org.gradle.java.home=C:/Users/Alfred/.jdks/openjdk-25.0.1
